program DwTraderBoot;

uses
  Forms,
  Principal in 'Principal.pas' {Form_Principal},
  Config in 'Config.pas' {Form_Config},
  BotaoCompra in 'BotaoCompra.pas' {Form_botaoCompra};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Dw TraderBoot';
  Application.CreateForm(TForm_Principal, Form_Principal);
  Application.Run;
end.
